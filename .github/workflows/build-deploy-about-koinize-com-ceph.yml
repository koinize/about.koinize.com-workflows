name: about.koinize.com (Build & Deploy)-Ceph

on:
  workflow_dispatch:
  workflow_run:
    workflows: ["about.koinize.com (Translate)"]
    types: [completed]

jobs:
  build:
    name: Build and Deploy Job
    runs-on: ubuntu-latest
    timeout-minutes: 180
    if: ${{ github.event_name == 'workflow_dispatch' || github.event.workflow_run.conclusion == 'success' }}

    steps:
      - name: Checkout workflows repo
        uses: actions/checkout@v4

      - name: Checkout about.koinize.com (private)
        uses: actions/checkout@v4
        with:
          repository: koinize/about.koinize.com
          path: about_repo
          token: ${{ secrets.REPO_TOKEN }}
          fetch-depth: 0
          submodules: true
          ref: main

      - name: Setup Hugo
        uses: peaceiris/actions-hugo@v2
        with:
          hugo-version: "0.123.8"

      - name: Build Hugo (keep-alive)
        working-directory: docs_repo
        shell: bash
        run: |
          ( while true; do echo "[$(date)] build alive"; sleep 900; done ) &
          KEEPALIVE_PID=$!
          hugo --logLevel debug --panicOnWarning --trace --printPathWarnings --printMemoryUsage
          kill $KEEPALIVE_PID

      # (Recommended) Ensure AWS CLI exists (ubuntu-latest usually has it, but this makes it deterministic)
      - name: Setup AWS CLI
        shell: bash
        run: |
          set -euo pipefail
          if ! command -v aws >/dev/null 2>&1; then
            sudo apt-get update -y
            sudo apt-get install -y awscli
          fi
          aws --version

      # --- Ceph S3 sync ---
      - name: Sync Website to Ceph S3 (aws s3 sync)
        shell: bash
        env:
          AWS_ENDPOINT_URL:       ${{ vars.AWS_ENDPOINT_URL }}
          AWS_DEFAULT_REGION:     ${{ vars.AWS_DEFAULT_REGION }}
          AWS_REGION:             ${{ vars.AWS_DEFAULT_REGION }}
          AWS_S3_FORCE_PATH_STYLE: "true"  # helpful for Ceph; harmless on AWS
          AWS_ACCESS_KEY_ID:      ${{ secrets.AWS_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY:  ${{ secrets.AWS_SECRET_ACCESS_KEY }}
        run: |
          set -euo pipefail

          SRC="docs_repo/public"
          DEST="s3://about-koinize-com"

          test -d "$SRC" || { echo "Missing build output: $SRC"; exit 1; }

          echo "Syncing: $SRC -> $DEST"
          aws s3 sync "$SRC" "$DEST" \
            --endpoint-url "$AWS_ENDPOINT_URL" \
            --only-show-errors \
            --delete
    
      # --- Final: Purge Bunny CDN cache for the deployed site ---
      - name: Purge Bunny CDN cache
        if: success()
        env:
          BUNNY_API_KEY: ${{ secrets.BUNNY_API_KEY }}
          BASE_URL:      "https://about.koinize.com"
        shell: bash
        run: |
          set -euo pipefail
          TARGET="${BASE_URL%/}/*"
          echo "Purging Bunny CDN for: $TARGET"
          curl -sS -G \
            -H "X-Api-Key: ${BUNNY_API_KEY}" \
            --data-urlencode "url=${TARGET}" \
            "https://api.dynabic.com/bn/purge?async=true" \
            -D - | sed -n '1,60p'